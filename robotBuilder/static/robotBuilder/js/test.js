import testComponent from './test_comp'
import ReactDOM from 'react-dom'

const element = <h1>Heo, world!</h1>;
const container = document.getElementById('popup_container');
// ReactDOM.render(element, container);
