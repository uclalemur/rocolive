/*
Subcomponent:
  maintains parameters and subcomponent parameters of a subcomponents
  params:
    subcomponent type
    parameters
    subcomponent parameters
    constraints
  state:
    position
  output:
    3D mesh object
      onHover: shows parameters hierarchily
      onClick: allows users to edit parameters of subcomponent
*/

import React, {Component} from 'react'


export default Subcomponent extends Component {
  constructor(params) {
    super(props);

    this.state = {

    }


  }
}
